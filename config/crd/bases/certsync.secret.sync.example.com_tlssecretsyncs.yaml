---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: tlssecretsyncs.certsync.secret.sync.example.com
spec:
  group: certsync.secret.sync.example.com
  names:
    kind: TLSSecretSync
    listKind: TLSSecretSyncList
    plural: tlssecretsyncs
    singular: tlssecretsync
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TLSSecretSync is the Schema for the tlssecretsyncs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TLSSecretSyncSpec defines the desired state of TLSSecretSync
            properties:
              copyAnnotations:
                default: true
                description: CopyAnnotations copies annotations from the source Secret
                  to targets.
                type: boolean
              copyLabels:
                default: true
                description: CopyLabels copies labels from the source Secret to targets.
                type: boolean
              pruneOnDelete:
                default: true
                description: PruneOnDelete deletes target Secrets when this resource
                  is deleted.
                type: boolean
              refreshPolicy:
                description: |-
                  RefreshPolicy controls when synchronization happens.
                  Mode: "OnChange" or "Periodic" (if Periodic, set periodSeconds > 0).
                properties:
                  mode:
                    description: Mode selects sync behavior.
                    enum:
                    - OnChange
                    - Periodic
                    type: string
                  periodSeconds:
                    description: PeriodSeconds is used only when Mode=Periodic.
                    format: int64
                    minimum: 1
                    type: integer
                required:
                - mode
                type: object
              sourceRef:
                description: SourceRef points to the source TLS Secret to copy from.
                properties:
                  name:
                    description: Name of the source Secret.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the source Secret.
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              targets:
                description: Targets lists the namespaces that should receive a copy
                  of the Secret.
                properties:
                  namespaces:
                    description: Target namespaces to copy secrets to
                    items:
                      type: string
                    minItems: 1
                    type: array
                    uniqueItems: true
                    x-kubernetes-list-type: atomic
                required:
                - namespaces
                type: object
            required:
            - sourceRef
            - targets
            type: object
          status:
            description: TLSSecretSyncStatus defines the observed state of TLSSecretSync
            properties:
              conditions:
                description: Conditions follow K8s conventions (e.g., Ready, Degraded).
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failures:
                description: |-
                  Failures lists only targets currently failing to sync.
                  Entries are unique per namespace and removed on recovery.
                items:
                  description: FailureEntry describes a target namespace that failed
                    to sync.
                  properties:
                    lastErrorTime:
                      description: LastErrorTime records when this failure last occurred.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable description of the
                        failure.
                      type: string
                    namespace:
                      description: Namespace of the failing target.
                      minLength: 1
                      type: string
                    reason:
                      description: Reason is a short, machine-readable code (e.g.,
                        NamespaceMissing, RBACDenied).
                      minLength: 1
                      type: string
                    retryCount:
                      description: RetryCount is how many consecutive reconcile attempts
                        failed for this namespace.
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - namespace
                  - reason
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              observedSourceResourceVersion:
                description: ObservedSourceResourceVersion is the last processed RV
                  of the source Secret.
                type: string
              summary:
                description: Summary provides aggregate counters for sync state.
                properties:
                  failed:
                    description: Failed is how many are currently failing (len(Failures)).
                    format: int32
                    minimum: 0
                    type: integer
                  lastSyncTime:
                    description: LastSyncTime is when the last reconciliation pass
                      finished.
                    format: date-time
                    type: string
                  synced:
                    description: Synced is how many are currently in sync.
                    format: int32
                    minimum: 0
                    type: integer
                  totalTargets:
                    description: TotalTargets is how many namespaces are desired.
                    format: int32
                    minimum: 0
                    type: integer
                required:
                - failed
                - synced
                - totalTargets
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
